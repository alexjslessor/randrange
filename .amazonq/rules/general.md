
if running python commands use the local environemnt: `source /home/hauwei/Dropbox/APPS/prefect/bmo-2/bin/activate`
If running arch-auth pytest unit tests., run them in the docker container with the command: `docker exec -it arch-auth pytest`