


# How to run Unit tests with pytest

Unit tests will be run inside the running docker containers

use the command below to run the unit tests.
- `docker exec -it arch-auth pytest`: Runs the unit tests inside the backend container


# frontend-react
Do not remove commented out code in the react frontend. Edit around the commented code but do not remove it.